#include "main.h"

/**
 * _isdigit - Checks for digits.
 *@c: int to compare with ASCII key column
 *
 *
 * Return: 1 if digit, 0 otherwise.
 */

int _isdigit(int c)
{
	return (c >= 48 && c <= 57);
}
