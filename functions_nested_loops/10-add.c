#include "main.h"

/**
 *add - additions two integers and returns the result.
 *@operand_a: first int to add
 *@operand_b: second int to add
 *
 * Return: the sum, as int
 */
int add(int operand_a, int operand_b)
{
	return (operand_a + operand_b);
}
